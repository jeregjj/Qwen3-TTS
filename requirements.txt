# Qwen3-TTS SAP AI Core Deployment Requirements
# Flask-based inference server with GPU support

# Web framework and production server
Flask==3.0.3
gunicorn==22.0.0
Werkzeug==3.0.3

# NOTE: torch and torchaudio are provided by base image (pytorch:2.3.0-cuda12.1)
# Do NOT install here - would break flash-attn wheel compatibility

# Qwen TTS model package (this pulls in the correct transformers version)
qwen-tts>=0.1.0

# Hugging Face ecosystem - let qwen-tts control transformers version
accelerate>=0.33.0
huggingface-hub>=0.24.5

# Audio processing
soundfile==0.12.1
librosa==0.10.2.post1
scipy==1.14.0

# Numerical computing
numpy>=1.26.4

# Additional dependencies
einops>=0.8.0
safetensors>=0.4.4
tokenizers>=0.19.1
sentencepiece>=0.2.0

# Utilities
requests>=2.32.3
pyyaml>=6.0.1
tqdm>=4.66.5
regex>=2024.7.24

# flash_attention_2 is installed via wheel in Dockerfile
